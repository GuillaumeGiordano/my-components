type $$ComponentProps = {
    /** Thickness of the bar — any CSS length. */
    height?: string;
    /** Fill color — any CSS color or token. */
    color?: string;
};
declare const ScrollProgressBar: import("svelte").Component<$$ComponentProps, {}, "">;
type ScrollProgressBar = ReturnType<typeof ScrollProgressBar>;
export default ScrollProgressBar;
